# stm32-hardware-validation-framework
Python-based embedded hardware validation framework demonstrating automated device testing of an STM32 microcontroller via UART. Includes firmware command protocol, BME280 sensor validation, structured test artifacts (CSV/JSON/Markdown), and evidence logging similar to production hardware validation systems.
